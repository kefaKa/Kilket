name: Tests

on:
    push:
        branches: [dev, main, test/all]
    pull_request:
        branches: [main]

jobs:
    build-and-test:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - name: Install dependencies
              run: |
                  sudo apt-get update
                  sudo apt-get install -y cmake ninja-build

            - name: Install vcpkg
              run: |
                  git clone https://github.com/microsoft/vcpkg.git
                  ./vcpkg/bootstrap-vcpkg.sh
                  echo "VCPKG_ROOT=$PWD/vcpkg" >> $GITHUB_ENV

            - name: Configure
              run: cmake -B build -DCMAKE_TOOLCHAIN_FILE=$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake

            - name: Build
              run: cmake --build build

            - name: Run tests
              run: cd build && ./test_config_manager && ./test_task_runner && ./test_session_logger && ./test_kilket_core

            - name: Install kilket binary
              run: |
                  sudo cmake --install build
                  which kilket || echo "kilket not found after install"

            - name: Run CLI tests
              run: bash cli/tests/test_cli.sh
